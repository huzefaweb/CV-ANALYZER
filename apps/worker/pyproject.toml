[project]
name = "cv-analyzer-worker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "psycopg[binary]==3.*",
    "pypdf>=6.15.0",
    "python-docx>=1.2.0",
    "pydantic>=2.9",
    "httpx>=0.27",
]

[dependency-groups]
dev = [
    "fpdf2>=2.8.8",
    "pytest>=9.1.1",
    "websockets>=17.0.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
markers = ["slow: deliberately slow real-timing tests (Story 1.5a)"]
